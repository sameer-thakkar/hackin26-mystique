name: Prod MB Status

on:
  schedule:
    - cron: '30 */6 * * *'

jobs:
  ensurer:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '12'
      - name: Mystique tests
        run: |
          yarn
          node ./tests/slack.js
