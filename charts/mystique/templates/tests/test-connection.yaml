apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mystique.fullname" . }}-test-connection"
  labels:
    {{- include "mystique.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mystique.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
