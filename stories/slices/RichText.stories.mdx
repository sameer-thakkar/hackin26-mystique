import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Slices/Rich Text" />

# Rich Text

- A simple slice with a repeatable zone of Rich Text fields.
- The Rich Text field is a configurable text field with formatting options.
- This field provides content writers with a WYSIWYG editor where they can define the text as a header or paragraph, make it bold, add links, etc.
