import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Slices/Unspace" />

# Unspace

Un-space removes the standard gap between slices, adding this between two slices will make the slice before and after it close to each other.
